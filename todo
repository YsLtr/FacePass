按当前代码状态整理（以仓库现状为准）

已实现

- [x] 增加调试用摄像头画面显示，由 `-d` 或 `--debug` 参数触发，实时显示面部识别、生物检测、欺诈识别、相似度等信息。`test` 参数启用时持续进行识别。`add` 时手动回车进行录入，`esc` 取消录入。`test` 时手动回车或 `esc` 结束测试。
- [x] 增加每用户人脸限制配置，默认为每用户 5 张脸。（当前实现更接近“每组最大人脸数”）
- [x] 单用户可添加不同的人脸组、可指定人脸组进行识别、可切换默认人脸组
- [x] 删除人脸的命令 `remove` 需要可以接受多参数：多序号、多标签
- [x] 查询应用规范配置：优先应用当前目录下的配置文件，再识别 `~/.config/facepass` 下的配置文件，最后识别 `/etc/facepass` 下的配置文件
- [x] 有效人脸检测：完整人脸、人脸距离远（屏幕占比低）
- [x] 要将裁切范围配置移到无关模型的位置
- [x] 需要有效帧达到配置需求，而不是固定帧数，可保留 `max_frame`
- [x] 参数值为 `0` 时的判定
- [x] 配置有效性检查和警告功能（当前已实现配置校验）
- [x] `--view` 参数打开画面预览
- [x] 增加 `q` 退出，`a` 添加
- [x] `--debug --view` 退出时显示 segmentation fault，需要修复
- [x] `status` 命令无法正确读取配置文件目录
- [x] 增加 `status` 前两个模型路径的显示
- [x] 只能识别一个人脸，可配置优先最大人脸还是中间人脸，涉及 `nms_threshold`
- [x] `test` 方框在活体检测失败时变为红色，实时显示多个人脸的匹配概率（当前红框已实现；多人脸匹配概率仍未完成）

部分实现 / 待补齐

- [-] arch package 撰写学习（当前已有 `PKGBUILD` 和 `facepass.install`，但仍需继续整理和验证）
- [-] 参考其他项目实现反欺诈（当前已有 `MiniFASNetV2`、`MiniFASNetV1SE` 和 `fusion` 模式）
- [-] 添加集成和卸载命令，实现自动集成到 `sudo` 等命令中，参考 LinuxCamPAM，适配 Ubuntu 和 Arch（当前已有 `enable` / `disable`，但只管理 systemd 服务，未自动改写 PAM 配置）

- [-] 快捷键取消人脸识别（临时和固定）。先评估实现难度和占用，若实时监测太耗内存可只实现临时打断识别（当前临时取消已具备，固定取消未完成）
- [-] 让项目支持 `pth` 还是将 `pth` 转换为 `onnx`（当前代码已统一使用 `onnx`，但未见 `pth` 支持或转换脚本）
- [-] 为add、remove、test添加对应的位置标签（已为add添加，remove和test待添加）

未实现

- [ ] 实现实时调用模型，而不是一直加载在内存当中
- [ ] 考虑其他模型来提高精准度
- [ ] 集成多种场景，如 noctalia lock 或者 hyprlock（可能）
- [ ] 增加用户人脸指定标签或序号进行测试 `test` 命令：为 `facepass test` 新增可重复传入的 `--face` 参数，支持按目标组内的人脸 selector 筛选测试目标。未指定 `--face` 时，保持当前“对整个目标组进行测试”的行为；指定 `--group` 时只在该组内解析 `--face`，否则默认使用当前默认组。若 `label` 命中多张人脸，则全部纳入候选；若索引越界、名称不存在或筛选后为空，需要返回明确错误信息，并在测试输出中显示当前实际参与匹配的人脸标签/数量。
- [ ] 统一 `user/group/face` 三层选择器设计：CLI 对外层级名称统一为 `user -> group -> face`，其中 `label` 仅作为 `face` 的属性，不作为独立层级。参数名称保持简洁，只使用 `--user`、`--group`、`--face`，其值统一视为 selector；selector 语法统一为 `#<index>` 表示按当前列表顺序的索引选择，其他值如`with-glassess`、`normal`表示按精确名称匹配，其中 `--face` 匹配精确 `label`。这样可以避免纯数字名称与索引冲突（需要修改当前项目为允许纯数字名称）。`add` 继续使用 `--label`，因为它是在创建 face；`remove`、`test`、后续可能的查询/切换类命令统一复用 selector 语法。`list` 等输出需要稳定展示各层级的 index，确保用户可以可靠地从列表复制 selector 进行下一步操作。

- [] 修复bug：list、remove、add等命令没有读取当前用户，而是直接使用默认用户ysltr进行操作

- [ ] 识别失败后可选截取图片并保存
- [ ] 重新设计人脸录入、检测方案，需要安全性更高的检测方案，针对每个人脸都重新训练一个差值，有效抵御照片、图片攻击：动态活体录入
- [ ] YuNet 可用，尝试替换掉 SFace，用 MobileFaceNet、GhostFaceNet，或实现多模型切换功能（需先进行兼容性考量）
- [ ] 识别时并行，加速识别 `timeout`

- [ ] 增加逐帧读取参数，`p` 下一帧，添加 `s` 退出逐帧读取
- [ ] 重新设计 `add` 逻辑，应该如何显示、如何进行、如何确认是否成功扫描
- [ ] 增加多个预设配置，可切换配置组。需要更改配置读取方式，文件名作为配置名。需要先询问配置读取方式和顺序
- [ ] 添加可配置的定时提醒（默认 72 小时）输入原密码
- [ ] `add` 和 `test` 中都存在关于 `--view` 的重复代码，需要将其移出
- [] 解决未使用的AuthResponse返回值warning
备注

- 需要多录入不同状态下的人脸
