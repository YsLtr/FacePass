- [] arch package 撰写学习

- [-] 参考其他项目实现反欺诈
- [] 实现实时调用模型，而不是一直加载在内存当中
- [] 考虑其他模型来提高精准度
- [] 集成多种场景，如noctalia lock或者hyprlock（可能）
- [] 添加集成和卸载命令，实现自动集成到sudo等命令中 参考 LinuxCamPAM，适配ubuntu和arch（enable已有命令）
- [x] 增加调试用摄像头画面显示，由 -d 或 --debug 参数触发，实时显示面部识别、生物检测、欺诈识别、相似度等信息。test参数启用时持续进行识别。add时手动回车进行录入，esc取消录入。test时手动回车或esc结束测试。

- [] 增加q键退出
- [] test方框在活体检测失败时变为红色，实时显示多个人脸的匹配概率

- [] 快捷键取消人脸识别（临时和固定）。先评估实现难度和占用，若实时监测太耗内存可只实现临时打断识别
- [x] 增加每用户人脸限制配置，默认为每用户5张脸
- [] 单用户可添加不同的人脸组、可指定人脸组进行识别、可切换默认人脸组
- [] 增加用户人脸指定标签或序号进行测试 test 命令
- [] 删除人脸的命令remove需要可以接受多参数：多序号、多标签

- [] 识别失败后可选截取图片并保存

- [] **查询应用规范配置** 优先应用当前目录下的配置文件，再识别~/.config/facepass 下的配置文件，最后识别 /etc/facepass 下的配置文件（安装时需要自动生成一个）

需要多录入不同状态下的人脸

- [] 重新设计人脸录入、检测方案，需要安全性更高的检测方案，针对每个人脸都重新训练一个差值，有效抵御照片、图片攻击 **动态活体录入**

- [x] 有效人脸检测： 完整人脸  人脸距离远（屏幕占比低）

- [] YuNet可用，尝试替换掉SFace，用Mobilefacenet、ghostfacenet 或 实现多模型切换功能（需先进行兼容性考量）
- [x] 要将裁切范围配置移到无关模型的位置
- [x] 让项目支持pth还是将pth转换为onnx
- [] add 时也显示画面，方便调整
- [] 识别时并行，加速识别 *timeout*


- [x] 需要有效帧达到配置需求，而不是固定帧数，可保留max_frame
- [x] 参数值为0时的判定
- [] 配置有效性检查和警告功能（检查当前配置校验功能）


- [] 只能识别一个人脸，可配置优先最大人脸还是中间人脸  # Non-maximum suppression threshold *nms_threshold*
- [] 增加逐帧读取debug参数, p下一帧，添加q退出逐帧读取
- [] --verbose参数需要匹配以显示更多信息

- [] 增加多个预设配置，可切换配置组 需要更改配置读取方式，文件名作为配置名。需要先询问配置读取方式和顺序

- [] 最后解决warning问题，审查优化代码，画出代码结构
