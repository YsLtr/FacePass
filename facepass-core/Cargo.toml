[package]
name = "facepass-core"
description = "Core library for FacePass face recognition authentication"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
# Serialization
serde = { workspace = true }
serde_json = { workspace = true }
bincode = { workspace = true }
toml = { workspace = true }

# Logging
log = { workspace = true }
env_logger = { workspace = true }

# Error handling
thiserror = { workspace = true }
anyhow = { workspace = true }

# UUID
uuid = { workspace = true }

# Image processing (includes DNN/ONNX via FaceDetectorYN/FaceRecognizerSF)
opencv = { workspace = true }

# System
libc = "0.2"

# Misc
once_cell = "1"
